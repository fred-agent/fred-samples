# The smallest Apache that is genuinely a WebDAV share: mod_dav over a folder,
# which is what mod_dav_fs means. Nothing here is tuned for production — it
# exists so this Knowledge Base can be run against the same server software it
# is written for, rather than against a stand-in.

ServerRoot /usr/local/apache2
Listen 80
ServerName localhost

LoadModule mpm_event_module   modules/mod_mpm_event.so
LoadModule unixd_module       modules/mod_unixd.so
LoadModule authn_core_module  modules/mod_authn_core.so
LoadModule authz_core_module  modules/mod_authz_core.so
LoadModule alias_module       modules/mod_alias.so
LoadModule dir_module         modules/mod_dir.so
LoadModule mime_module        modules/mod_mime.so
LoadModule log_config_module  modules/mod_log_config.so
LoadModule dav_module         modules/mod_dav.so
LoadModule dav_fs_module      modules/mod_dav_fs.so

User daemon
Group daemon

TypesConfig conf/mime.types
ErrorLog /proc/self/fd/2
LogLevel warn
LogFormat "%h %l %u %t \"%r\" %>s %b" common
CustomLog /proc/self/fd/1 common

# mod_dav needs somewhere to keep locks even when nothing ever takes one.
DavLockDB /var/lock/apache2/DavLock

DocumentRoot /usr/local/apache2/htdocs
Alias /dav /srv/share

<Directory /srv/share>
    Dav On
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>
